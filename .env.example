# Supabase Configuration
# Get these values from your Supabase project settings at https://app.supabase.com
EXPO_PUBLIC_SUPABASE_URL=your_supabase_url_here
SUPABASE_URL=your_supabase_url_here
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase Service Role Key (server-side only - NEVER expose to client)
# Required for server-side API routes that need to bypass Row Level Security
# Get this from: Supabase Dashboard > Settings > API > service_role key
# WARNING: This key has admin privileges. Only use in secure server environments.
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Stripe Configuration (Server-side keys - NEVER expose to client)
# Get these from: Stripe Dashboard > Developers > API keys
# WARNING: Keep secret keys server-side only
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret_here

# Stripe Configuration (Client-side - safe to expose)
# Publishable key can be safely used on the client
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# Stripe Price IDs for subscription plans
# Create products/prices in Stripe Dashboard > Products
# Copy the price IDs (start with price_) here
NEXT_PUBLIC_STRIPE_PRICE_MONTHLY=price_your_monthly_price_id_here
NEXT_PUBLIC_STRIPE_PRICE_ANNUAL=price_your_annual_price_id_here

# Stripe Payment Links (legacy - used by old paywall buttons)
# Create Payment Links in Stripe for each plan and paste the full URL here
EXPO_PUBLIC_STRIPE_WEEKLY_LINK=https://buy.stripe.com/your_weekly_link
EXPO_PUBLIC_STRIPE_MONTHLY_LINK=https://buy.stripe.com/your_monthly_link
EXPO_PUBLIC_STRIPE_LIFETIME_LINK=https://buy.stripe.com/your_lifetime_link

# OpenAI API Key (server-side only - set in Supabase Edge Function secrets)
# Do NOT add EXPO_PUBLIC_ prefix - this should never be exposed to client
OPENAI_API_KEY=your_openai_api_key_here
